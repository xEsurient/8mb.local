# GPU overlay — merge with the default file:
#   docker compose -f docker-compose.yml -f docker-compose.gpu.yml up -d --build
services:
  8mblocal:
    gpus: all
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
      - NVIDIA_DRIVER_CAPABILITIES=compute,video,utility
